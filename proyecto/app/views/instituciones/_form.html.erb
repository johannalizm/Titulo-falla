<%= form_with(model: institucione, local: true) do |form| %>
  <% if institucione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(institucione.errors.count, "error") %> prohibited this institucione from being saved:</h2>

      <ul>
      <% institucione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :pais %>
    <%= form.text_field :pais %>
  </div>

  <div class="field">
    <%= form.label :codigopostal %>
    <%= form.text_field :codigopostal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
